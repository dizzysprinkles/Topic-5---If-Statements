namespace Topic_5___If_Statements
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Print Good Job if variable grade is higher/equal than 80
            int grade, radius, number;

            number = 7;

            if (number >= 5 && number <= 15)
            {
                Console.WriteLine("This code actually worked!");
            }


            //Console.WriteLine("Hey there! What is your current grade average (whole numbers) ?");
            //Int32.TryParse(Console.ReadLine(), out grade);

            //if (grade >= 80)
            //{
            //    Console.WriteLine("Good Job! That is a great grade average to have!");
            //}
            //else
            //{
            //    Console.WriteLine("Study harder!");
            //}

            //Console.WriteLine("Give me a radius");
            //Int32.TryParse(Console.ReadLine(), out radius);
            //if (radius <0)
            //{
            //    Console.WriteLine("Negative radii are illegal!");
            //}
            //else if (radius == 0)
            //{
            //    Console.WriteLine("You have a dot.");
            //}
            //else
            //{
            //    Console.WriteLine("The diameter is " + radius*2 + ".");
            //}
            
        }
    }
}
